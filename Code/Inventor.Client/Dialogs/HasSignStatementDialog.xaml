<Window x:Class="Inventor.Client.Dialogs.HasSignStatementDialog"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		Title="Has Sign" SizeToContent="WidthAndHeight" MinWidth="100" WindowStartupLocation="CenterOwner">
	<DockPanel LastChildFill="True">
		<StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="5">
			<Button Margin="5" Padding="5,2,5,2" Click="okClick">OK</Button>
			<Button Margin="5" Padding="5,2,5,2" Click="cancelClick">Cancel</Button>
		</StackPanel>
		<StackPanel Orientation="Vertical" Name="_contextControl">
			<GroupBox Header="Concept">
				<ComboBox Name="_comboBoxConcept" SelectedItem="{Binding Path=Concept, Mode=TwoWay}" />
			</GroupBox>
			<GroupBox Header="Sign">
				<ComboBox Name="_comboBoxSign" SelectedItem="{Binding Path=Sign, Mode=TwoWay}" />
			</GroupBox>
		</StackPanel>
	</DockPanel>
</Window>
