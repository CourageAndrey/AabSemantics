<Window x:Class="Inventor.Client.Dialogs.ConceptDialog"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:controls="clr-namespace:Inventor.Client.Controls"
		Title="Edit Concept" SizeToContent="WidthAndHeight" MinHeight="300" MinWidth="100" WindowStartupLocation="CenterOwner">
	<DockPanel LastChildFill="True">
		<StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="5">
			<Button Margin="5" Padding="5,2,5,2" Click="okClick">OK</Button>
			<Button Margin="5" Padding="5,2,5,2" Click="cancelClick">Cancel</Button>
		</StackPanel>
		<Grid Name="_contextControl">
			<Grid.RowDefinitions>
				<RowDefinition Height="*"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>
			<GroupBox Grid.Row="0" Header="Name">
				<controls:LocalizedStringVariableControl EditValue="{Binding Path=Name, Mode=OneTime}" x:Name="_nameConrol" />
			</GroupBox>
			<GroupBox Grid.Row="1" Header="Hint">
				<controls:LocalizedStringVariableControl EditValue="{Binding Path=Hint, Mode=OneTime}" x:Name="_hintConrol" />
			</GroupBox>
		</Grid>
	</DockPanel>
</Window>
